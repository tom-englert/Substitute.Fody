version: 1.3.{build}
image: Visual Studio 2017
configuration: Release
platform: Any CPU
#install:
#- cmd: git submodule update --init --recursive
before_build:
- cmd: nuget restore Substitute.sln
build:
  project: Substitute.sln
  verbosity: minimal
artifacts:
- path: Deploy/*.nupkg